// variables.tf moved to resource/variables.tf — keep this stub to avoid breakage
// If you run Terraform from the `resource/` directory, the active variables file
// is `resource/variables.tf`. Remove this stub when you're confident.
